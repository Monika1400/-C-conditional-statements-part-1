#include<stdio.h>
int main() {
    char ch;
    scanf("%d",&ch);
    if ((ch>=65)&&(ch<=90)||(ch>=97)&&(ch<=122)){
        printf("alphabets");
    }
    else if((ch>=48)&&(ch<=57)){
    printf("Digits");
    }
    else {
        printf("Special characters");
    }
    return 0;
}