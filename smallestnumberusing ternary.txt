#include<stdio.h>
int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if ((a>b&&b<a)?a:b) {
        printf("%d is the smallest");
    }
    else {
        printf("%d is largest");
    }
    return 0;
}